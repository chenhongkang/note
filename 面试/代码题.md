1.sum(1)(2)(3)

```javascript
function sum (pre) {
    var Sum = pre
    var closeFunc = function(next) {
        Sum += next
        return closeFunc
    }
    closeFunc.toString = function () { return Sum }
    return closeFunc
}
```

